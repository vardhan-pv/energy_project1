{
  "name": "energy-project-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd dashboard && npm install && npm run build && rm -rf ../.vercel/output && cp -r .vercel/output ../"
  }
}
